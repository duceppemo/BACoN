[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bacon-nanopore"
# Static on purpose (see mashID): tests/test_cli.py keeps this in sync with bacon.__version__.
version = "0.3.0"
description = "Bait, Assemble and Compare Nanopore reads matching a reference sequence, such as an organelle genome."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Marc-Olivier Duceppe" }]
requires-python = ">=3.10"
dependencies = []
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]

[project.optional-dependencies]
fast = ["isal>=1.0"]      # faster gzip decompression of reads
test = ["pytest>=7", "pytest-cov>=5", "ruff", "pre-commit"]

[project.urls]
Homepage = "https://github.com/duceppemo/BACoN"
Documentation = "https://github.com/duceppemo/BACoN/wiki"

[project.scripts]
bacon = "bacon.cli:main"

[tool.setuptools]
packages = ["bacon"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["tools: needs the external programs (minimap2, filtlong, flye, ska, ...) on PATH"]

[tool.coverage.run]
source = ["bacon"]

[tool.ruff]
line-length = 115
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E731", "B905"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]
